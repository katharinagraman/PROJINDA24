/**
 * Remembers state of the program where user is
 * But each part has to be a "room then"
 * There are two rooms, home page and day page.
 * If I make them extend pages planner and day then that could work
 */

public class State {
    private Page currentPage;


    public State(Page homepage){
        currentPage = homepage;
    }



    public Page getCurrentPage(){
        if currentPage.

    }


}
